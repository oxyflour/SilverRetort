[project]
name = "lerobot-record-simulation-skill"
version = "0.1.0"
description = "Record ovphysx ROS simulations as local LeRobot datasets"
requires-python = ">=3.10,<3.11"
dependencies = [
    "lerobot>=0.4.3,<0.5",
    "numpy>=1.26,<3",
]

[tool.uv]
package = false
