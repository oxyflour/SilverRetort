[project]
name = "verify-robot-usd-skill"
version = "0.1.0"
description = "Standalone ovphysx numerical stability gate for robot USD files"
requires-python = ">=3.10,<3.11"
dependencies = [
    "numpy>=2.0,<3",
    "ovphysx>=0.4.13,<0.5",
]

[tool.uv]
package = false
