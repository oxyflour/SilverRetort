[project]
name = "silverretort-hermes"
version = "0.1.0"
description = "hermes-agent gateway launcher for SilverRetort desktop"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "aiohttp>=3.14.1",
    "hermes-agent[web]>=0.14.0",
    "mcp>=1.26.0",
    "pyyaml>=6.0",
]
